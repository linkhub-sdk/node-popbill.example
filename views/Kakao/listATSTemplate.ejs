<!DOCTYPE html>
<html>
<head>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <title>팝빌 SDK Express Example. </title>
</head>
<body>
<div id="content">
    <p class="heading1">Response</p>
    <br/>
    <fieldset class="fieldset1">
        <legend><%= path %></legend>
        <% result.forEach(function(template, index){ %>
        <fieldset class="fieldset2">
            <legend> 템플릿목록 [ <%= index + 1 %> / <%= result.length %> ]</legend>
            <ul>
                <li> templateCode (템플릿 코드) : <%= template.templateCode %></li>
                <li> templateName (템플릿 제목) : <%= template.templateName %></li>
                <li> template (템플릿 내용) : <%= template.template %></li>
                <li> plusFriendID (템플릿 플러스친구 아이디) : <%= template.plusFriendID %></li>

                <% if (template.btns) { %>
                <% template.btns.forEach(function (btn, index) { %>
                <fieldset class="fieldset3">
                    <legend>버튼목록 [ <%= index + 1 %> / <%= template.btns.length %> ]</legend>
                    <ul>
                        <li> 버튼명 (n) : <%= btn.n %> </li>
                        <li> 버튼유형 (t) <%= btn.t %> </li>
                        <li> [앱링크] Android [웹링크] Mobile (u1) : <%= btn.u1 %></li>
                        <li> [앱링크] IOS [웹링크] PC (u2) : <%= btn.u2 %></li>
                    </ul>
                </fieldset>
                <% });} %>

            </ul>
        </fieldset>
        <% }); %>
    </fieldset>
</div>
</body>
</html>